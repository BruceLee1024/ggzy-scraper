---
name: ggzy-bid-scraper
description: 从全国公共资源交易平台(ggzy.gov.cn)抓取招标/中标公示等数据。Hermes引导用户确认查询条件后自动执行抓取，生成Excel。触发词：招标、公共资源交易、ggzy、投标、中标、政府采购、查招标、抓招标数据。
version: 1.0.0
author: BruceLee1024
license: MIT
metadata:
  hermes:
    tags: [招标, 公共资源交易, 数据抓取, Excel, 政府采购, 对话引导]
prerequisites:
  python_packages: [playwright, openpyxl]
  system_tools: [playwright chromium]
---

# ggzy-bid-scraper — Hermes 对话引导手册

## 一、何时激活本 Skill

用户说出以下任意内容时激活：
- "帮我查一下招标 / 招标公告 / 招标数据"
- "查一下 ggzy / 公共资源交易平台"
- "抓一下（某省/全国）的（工程/政府采购）招标"
- "最近有什么招标" / "查一下投标截止时间"
- "帮我看看中标结果 / 中标公示"
- "下载招标 Excel"

---

## 二、对话引导流程

用户触发后，**依次向用户确认以下参数**，每次一个问题，允许用户跳过（使用默认值）。已从用户话语中能推断的参数直接跳过，不用再问。

### 问题 1 — 业务类型

> "你想查哪类交易？（默认：工程建设）"

| 用户说 | 参数 `biz` | 脚本 ID |
|--------|-----------|---------|
| 工程 / 工程建设 / 建设工程 | `1` | `01` |
| 政府采购 / 采购 | `2` | `02` |
| 土地 / 土地使用权 | `3` | `03` |
| 矿业 / 矿业权 | `4` | `04` |
| 国有产权 / 国资 | `5` | `05` |
| 全部 / 不限 / 所有 | `6` | `00` |
| （未提及）| `1`（默认） | `01` |

### 问题 2 — 省份

> "查哪个省？（默认：全国）"

| 用户说 | `province_key` |
|--------|---------------|
| 全国 / 不限 / 所有省 / 全部 | `0` |
| 北京 | `1` |
| 上海 | `9` |
| 广东 | `19` |
| 云南 | `25` |
| 四川 | `23` |
| 浙江 | `11` |
| 江苏 | `10` |
| 山东 | `15` |
| 湖南 | `18` |
| 湖北 | `17` |
| 河南 | `16` |
| 陕西 | `27` |
| 其他省份 | 参考脚本中 PROVINCES 字典 |
| （未提及）| `0`（默认，全国） |

### 问题 3 — 时间范围

> "查多久内发布的？（默认：近三天）"

| 用户说 | `time_key` |
|--------|-----------|
| 今天 / 当天 / 今日 | `1` |
| 最近 / 近三天 / 三天 / 这几天 | `2`（默认） |
| 近十天 / 十天 / 最近一周多 | `3` |
| 近一个月 / 本月 / 这个月 | `4` |
| 近三个月 / 最近三月 | `5` |

### 问题 4 — 数量

> "大概要多少条？（默认：3页约60条）"

| 用户说 | `pages` | `details` |
|--------|---------|-----------|
| 随便看看 / 少一点 / 测试 | `1` | `5` |
| 默认 / 正常 / 适量 | `3` | `20` |
| 多一些 / 较多 | `10` | `50` |
| 全量 / 尽量多 / 所有 | `50` | `100` |
| 只要列表不要详情 | `3` | `0` |

### 问题 5 — 是否需要详情字段

> "需要预算金额、投标截止时间这些详情吗？（默认：需要）"

- **需要 / 是 / 要**（默认）→ `details > 0`，抓取预算、截止时间、资金来源等
- **不需要 / 否 / 只要列表**→ `details = 0`，只抓列表字段

---

## 三、确认后执行

确认参数后，**向用户展示摘要，然后执行脚本**：

```
好的，我来帮你抓取：
- 业务类型：{业务名称}
- 省份：{省份名称}
- 时间范围：{时间描述}
- 数量：约 {pages×20} 条，其中 {details} 条含详情
正在启动抓取（无头浏览器模式，无需用户操作）...
```

**执行命令**（在 skill 目录下运行）：

```bash
python3 scripts/ggzy_scraper_simple.py \
  -b {biz} -p {pages} -d {details} \
  -v {province_key或中文名} -t {time_key} -s {stage_key} \
  [--headless] [-o 输出路径]
```

常用示例：
```bash
# 工程建设，全国，近三天，3页，20条详情，无头模式
python3 scripts/ggzy_scraper_simple.py -b 1 -p 3 -d 20 --headless

# 政府采购，广东，近十天，10页，不抓详情
python3 scripts/ggzy_scraper_simple.py -b 2 -v 广东 -t 3 -p 10 -d 0

# 指定输出路径
python3 scripts/ggzy_scraper_simple.py -b 1 -p 5 -d 30 -o ~/Documents/招标.xlsx
```

> **脚本目录**：skill 安装后的 `scripts/` 目录。

**执行时提示用户**：抓取需要 1-3 分钟，浏览器在后台运行，无需操作。

---

## 四、结果解读与展示

脚本完成后，读取 `/tmp/ggzy_records.json` 和终端输出，向用户展示：

```
✅ 抓取完成！
- 共获取 XX 条招标公告
- 其中含预算金额：XX 条
- Excel 已保存到桌面：~/Desktop/招标公告_数据.xlsx

典型条目（前3条）：
1. [标题] | 预算：XX万 | 截标：YYYY-MM-DD | [省份]
2. ...
3. ...

如需查看更多或按条件筛选，可在 Excel 中使用筛选功能。
```

读取前3条示例：

```python
import json
recs = json.load(open('/tmp/ggzy_records.json'))
for r in recs[:3]:
    print(r.get('title',''), r.get('budget_money',''), r.get('bid_deadline',''))
```

---

## 五、常见追问处理

| 用户追问 | Hermes 操作 |
|---------|------------|
| "再多抓一些" | 重新执行，`-p` 翻倍 |
| "只要广东的" | 重新执行，加 `-v 广东` |
| "只要云南近十天的" | 加 `-v 云南 -t 3` |
| "只要有预算金额的" | 执行后过滤：`[r for r in recs if r.get('budget_money')]` |
| "导出 Excel 到指定位置" | 加 `-o /path/to/file.xlsx` |
| "帮我看看中标结果" | 加 `-s 3`（中标结果公示） |
| "帮我看看中标候选人" | 加 `-s 2` |

---

## 六、WAF / 异常处理

| 脚本输出 | 含义 | 处理方式 |
|---------|------|---------|
| `✅ WAF 检测通过` | 正常 | 继续 |
| `⚠️ 无头模式被WAF拦截` | 隐身失败 | 去掉 `headless` 参数，改用有头模式（会弹 Chromium 窗口） |
| `⚠️ 页面数据为空，可能被限流` | IP 被临时标记 | 等待 5-10 分钟后重试；或告知用户 |
| `⚠️ 详情抓取失败` | 个别详情页超时 | 正常现象，跳过该条，不影响其他数据 |
| `第N页: 无数据，停止` | 总量不足设定页数 | 正常，已抓完全部数据 |

---

## 七、字段速查

### 列表字段
| 字段 | 说明 |
|------|------|
| `title` | 公告标题 |
| `publishTime` | 发布时间 |
| `provinceText` | 省份 |
| `businessTypeText` | 业务类型 |
| `informationTypeText` | 信息类型（招标/资审公告 等） |
| `industryTypeText` | 行业分类 |
| `url` | 详情页链接 |

### 详情字段（需 details > 0）
| 字段 | 含义 |
|------|------|
| `budget_money` | 预算金额（万元） |
| `deposit_money` | 投标保证金（万元） |
| `bid_deadline` | 投标截止时间 |
| `doc_deadline` | 招标文件获取截止 |
| `funding_source` | 资金来源 |
| `scale` | 建设规模 |
| `plan_days` | 计划工期（天） |
| `tender_agent` | 招标代理机构 |

---

## 八、技术备注（供调试参考）

- **WAF 绕过**：无头隐身模式（隐身补丁 + `--disable-blink-features=AutomationControlled`）+ URL 参数过滤，2026-05-25 实测列表+详情全部可用
- **过滤机制**：`DEAL_CLASSIFY` / `DEAL_STAGE` 通过 URL 参数服务端过滤；省份/时间通过 Vue 实例注入
- **详细技术文档**：`references/waf-bypass.md`
